module main;

class Test {
    let mut value;
}

fn main() {
    let count = {
        "bob": 0,
        "karen": 1
    };

    count.set("bob", count["bob"] + 1);
    count.set("karen", count["karen"] + 1);

    println(count);
}
