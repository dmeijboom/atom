class File {
    let name;
    let mode;

    fn test() {
        return [1, 2, 3].first();
    }
}

fn main() {
    let file = File(
        name: "test.txt",
        mode: 777
    );

    println(file.test());
}
