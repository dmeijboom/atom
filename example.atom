class User {
    let mut name;
    let age = 1;
}

fn newUser() {
   return User(
       name: "Dillen"
   );
}

fn main() {
    let user = newUser();

    user.name = "Gerard";

    println(user.name);
}
