module main;

class File {
    let name;
}

fn main() {
    let files = [File(name: "test.txt"), File(name: "etc"), File(name: "home")];

    for files {
        if .name.upper().contains("TXT") {
            println(.name.split("."));
        }
    }
}
