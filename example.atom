class File {
    let name;
    let mode;

    fn test() {
        return name;
    }
}

fn main() {
    let file = File(
        name: "test.txt",
        mode: 777
    );

    println(file.test());
}
