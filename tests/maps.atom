fn simple(t) {
    let map = {};

    map.set("name", "atom");

    t.assert("atom", map.get("name")!);
}

fn addValue(t) {
    let map = {};

    map.set("0", 10);

    t.assert(20, 10 + map.get("0")!);
}

pub fn mapsTests() {
    return [
        simple,
        addValue
    ];
}