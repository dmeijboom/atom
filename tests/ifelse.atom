fn nested(t) {
    let mut result = 10;

    if true {
        if false {
        } else {
            if !true {
            } else if true {
                result = 25;
            } else {
            }
        }
    }

    t.assert(25, result);
}

pub fn ifElseTests() {
    return [
        nested
    ];
}