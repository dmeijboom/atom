extern fn repr(value);
extern fn println(value);

class Array {
    extern fn push(self, item);
    extern fn pop(self);
    extern fn concat(self, other);
    extern fn len(self);
    extern fn cap(self);
}

class Str {
    extern fn len(self);
    extern fn upper(self);
    extern fn lower(self);
    extern fn concat(self, other);
}
