fn example(inner) {
    return inner();
}

fn test(outer) {
    return example(outer("hello"));
}

fn main() {
    let last = '!';
    let outer = |greeting| {
        let suffix = "world";

        return || {
            return f"{greeting} {suffix}{last}";
        };
    };

    return test(outer);
}
