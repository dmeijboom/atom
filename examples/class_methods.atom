class Test {
    let value;

    fn test() {
        return this.value;
    }

    fn example() {
        return this.test() + this.value;
    }

    fn foo() {
        return this.example() + this.test() + this.value;
    }
}

fn main() {
    let test = Test(value: 25);

    return test.foo();
}
